language: csharp
install:
- dotnet tool install -g Wyam.Tool
# deploy:
#   provider: pages
#   skip_cleanup: true
#   github_token: $GITHUB_TOKEN
#   local_dir: _site
#   target_branch: master
#   on:
#     branch: source